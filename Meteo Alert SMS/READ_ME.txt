This program notifies with an SMS the user if today is going to rain.
It uses the Open Weather Map to fetch the weather data, and the Twilio API to send an SMS to the user.
The program is perfect to be run with online tools like Python Anywhere to schedule the start every morning.